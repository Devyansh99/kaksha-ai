---
phase: 04-quality-signals-and-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/taxonomy_normalization.py
  - src/pipeline/report_pipeline.py
  - tests/test_quality_signals.py
autonomous: true
requirements:
  - QLT-01
  - QLT-03
must_haves:
  truths:
    - "User can see free-text misconception labels normalized to a fixed taxonomy"
    - "User can view confidence score and confidence band for each identified misconception"
  artifacts:
    - path: "src/pipeline/taxonomy_normalization.py"
      provides: "Deterministic taxonomy mapping rules and fallback behavior"
    - path: "src/pipeline/report_pipeline.py"
      provides: "Report payload with normalized labels and confidence visibility fields"
    - path: "tests/test_quality_signals.py"
      provides: "Automated quality-signal tests for normalization and confidence outputs"
  key_links:
    - from: "src/pipeline/report_pipeline.py"
      to: "src/pipeline/taxonomy_normalization.py"
      via: "Maps raw labels into normalized taxonomy fields"
    - from: "src/pipeline/report_pipeline.py"
      to: "tests/test_quality_signals.py"
      via: "Deterministic normalization and confidence assertions"
---

<objective>
Implement deterministic taxonomy normalization and confidence visibility in teacher report outputs.

Purpose: Deliver trustworthy, interpretable quality signals for teachers on top of Phase 3 report structure.
Output: Taxonomy normalization module, report integration, and QLT-01/QLT-03 tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-quality-signals-and-evaluation/04-CONTEXT.md
@.planning/phases/04-quality-signals-and-evaluation/04-RESEARCH.md
@.planning/phases/04-quality-signals-and-evaluation/04-VALIDATION.md
@.planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create deterministic taxonomy normalization contract</name>
  <files>src/pipeline/taxonomy_normalization.py</files>
  <read_first>.planning/phases/04-quality-signals-and-evaluation/04-CONTEXT.md, src/pipeline/report_pipeline.py, src/pipeline/misconception_extractor.py</read_first>
  <behavior>
    - Test 1: Alias labels map to deterministic canonical labels and taxonomy groups.
    - Test 2: Unknown labels map to Uncategorized with deterministic reason marker.
  </behavior>
  <action>Create `src/pipeline/taxonomy_normalization.py` with a versioned canonical taxonomy constant, alias lookup map, and `normalize_label(raw_label: str) -> dict` that returns keys `raw_label`, `normalized_label`, `taxonomy_group`, `normalization_reason`. Implement deterministic matching precedence: exact canonical, lowercase canonical, alias map, fallback to `Uncategorized` + `no_taxonomy_match`.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_quality_signals.py::test_normalize_label_maps_aliases_deterministically -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/taxonomy_normalization.py` exports `normalize_label`.
    - File contains literal fallback values `Uncategorized` and `no_taxonomy_match`.
    - Mapping path is deterministic and does not call external services.
  </acceptance_criteria>
  <done>QLT-01 normalization contract exists with deterministic behavior and fallback handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate normalized labels and confidence visibility in report output</name>
  <files>src/pipeline/report_pipeline.py</files>
  <read_first>src/pipeline/report_pipeline.py, src/pipeline/taxonomy_normalization.py, .planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Report includes raw and normalized taxonomy fields per misconception.
    - Test 2: Report includes rounded confidence and confidence band values.
  </behavior>
  <action>Update `build_teacher_report` flow in `src/pipeline/report_pipeline.py` to enrich each misconception record with `raw_label`, `normalized_label`, `taxonomy_group`, `normalization_reason`, `confidence_rounded` (2 decimals), and `confidence_band` using thresholds: low < 0.40, medium 0.40-0.74, high >= 0.75. Keep existing deterministic ordering and status filtering behavior unchanged.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_quality_signals.py::test_report_includes_normalized_labels_and_confidence_bands -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/report_pipeline.py` contains fields `normalized_label`, `taxonomy_group`, `confidence_band`.
    - Band thresholds appear as explicit numeric boundaries `0.40` and `0.75`.
    - Existing status include/exclude behavior remains based on included statuses and retry_exhausted exclusion for confidence math.
  </acceptance_criteria>
  <done>QLT-03 confidence visibility is present and deterministic in teacher report outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic quality-signal tests for normalization and confidence</name>
  <files>tests/test_quality_signals.py</files>
  <read_first>src/pipeline/taxonomy_normalization.py, src/pipeline/report_pipeline.py, tests/test_teacher_report_aggregation.py, .planning/phases/04-quality-signals-and-evaluation/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Normalization aliases and unknown labels resolve deterministically.
    - Test 2: Confidence rounding and band boundaries are enforced.
  </behavior>
  <action>Create `tests/test_quality_signals.py` containing test names exactly `test_normalize_label_maps_aliases_deterministically` and `test_report_includes_normalized_labels_and_confidence_bands`. Include boundary cases at confidence values 0.39, 0.40, 0.74, 0.75 and assert expected bands low/medium/high. Assert unknown labels map to `Uncategorized` with reason `no_taxonomy_match`.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_quality_signals.py::test_normalize_label_maps_aliases_deterministically tests/test_quality_signals.py::test_report_includes_normalized_labels_and_confidence_bands -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_quality_signals.py` contains both required test function names.
    - Tests assert raw and normalized taxonomy fields plus confidence bands.
    - Targeted test command exits with code 0.
  </acceptance_criteria>
  <done>QLT-01 and QLT-03 are protected by deterministic automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model-derived labels -> taxonomy mapper | Untrusted free-text labels cross into normalization logic |
| report quality fields -> teacher interpretation | Confidence and taxonomy signals influence instructional decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | src/pipeline/taxonomy_normalization.py | mitigate | Use deterministic lookup precedence and explicit fallback reason to prevent arbitrary label manipulation |
| T-04-02 | I | src/pipeline/report_pipeline.py | mitigate | Enforce fixed confidence-band thresholds and retain raw_label for auditability |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_quality_signals.py::test_normalize_label_maps_aliases_deterministically tests/test_quality_signals.py::test_report_includes_normalized_labels_and_confidence_bands -q
- [ ] venv\Scripts\python -m pytest tests/test_quality_signals.py -q
</verification>

<success_criteria>
- QLT-01 normalization and QLT-03 confidence visibility are fully represented in code and tests
- Quality fields remain deterministic across repeated runs
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-signals-and-evaluation/04-01-SUMMARY.md`
</output>
