---
phase: 04-quality-signals-and-evaluation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/pipeline/misconception_prompt.py
  - src/pipeline/strategy_comparison.py
  - tests/test_prompt_strategy_comparison.py
autonomous: true
requirements:
  - QLT-02
must_haves:
  truths:
    - "User can compare two prompting strategies and review a short recorded summary"
  artifacts:
    - path: "src/pipeline/misconception_prompt.py"
      provides: "Deterministic builders for zero-shot and few-shot strict JSON prompts"
    - path: "src/pipeline/strategy_comparison.py"
      provides: "Deterministic strategy comparison and summary generation"
    - path: "tests/test_prompt_strategy_comparison.py"
      provides: "Automated tests for strategy comparison metrics and stability"
  key_links:
    - from: "src/pipeline/strategy_comparison.py"
      to: "src/pipeline/misconception_prompt.py"
      via: "Runs both prompt strategies on the same deterministic input slice"
    - from: "src/pipeline/strategy_comparison.py"
      to: "tests/test_prompt_strategy_comparison.py"
      via: "Asserts required metrics and deterministic summary output"
---

<objective>
Implement deterministic prompt strategy comparison and summary artifact generation.

Purpose: Provide evidence-backed strategy comparison required for quality trust and future prompt tuning.
Output: dual prompt builders, comparison evaluator, and QLT-02 test suite.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-quality-signals-and-evaluation/04-CONTEXT.md
@.planning/phases/04-quality-signals-and-evaluation/04-RESEARCH.md
@.planning/phases/04-quality-signals-and-evaluation/04-VALIDATION.md
@.planning/phases/04-quality-signals-and-evaluation/04-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic zero-shot and few-shot prompt builders</name>
  <files>src/pipeline/misconception_prompt.py</files>
  <read_first>src/pipeline/misconception_prompt.py, .planning/phases/04-quality-signals-and-evaluation/04-CONTEXT.md, tests/test_resilient_misconception_extraction.py</read_first>
  <behavior>
    - Test 1: Both strategy prompts enforce strict JSON-only contract.
    - Test 2: Strategy prompts are distinguishable and deterministic.
  </behavior>
  <action>Update `src/pipeline/misconception_prompt.py` to export `build_misconception_prompt_zero_shot(row: dict) -> str` and `build_misconception_prompt_few_shot(row: dict) -> str` while preserving strict JSON output instructions and required keys. Keep existing `build_misconception_prompt` as deterministic wrapper to zero-shot for backward compatibility.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_prompt_strategy_comparison.py::test_prompt_strategy_builders_are_distinct_and_json_strict -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/misconception_prompt.py` exports zero-shot and few-shot builder functions.
    - Existing `build_misconception_prompt` symbol remains available.
    - Both prompt builders contain strict JSON contract instructions and required key list.
  </acceptance_criteria>
  <done>QLT-02 strategy inputs are available as deterministic prompt builders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic strategy comparison and summary writer</name>
  <files>src/pipeline/strategy_comparison.py</files>
  <read_first>src/pipeline/misconception_prompt.py, src/pipeline/misconception_extractor.py, src/pipeline/taxonomy_normalization.py, .planning/phases/04-quality-signals-and-evaluation/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Comparison output includes all required metrics with deterministic ordering.
    - Test 2: Summary artifact content is stable across repeated runs on same input.
  </behavior>
  <action>Create `src/pipeline/strategy_comparison.py` with `compare_prompt_strategies(rows: list[dict], analyzer) -> dict` and `write_strategy_summary(result: dict, output_path: str = ".planning/phases/04-quality-signals-and-evaluation/04-AB-RESULTS.md") -> None`. Metrics must include `parse_success_rate`, `normalized_label_coverage`, `rerun_consistency`, and `avg_confidence` for each strategy. Use deterministic row ordering and deterministic tie-break ordering when ranking strategies.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_prompt_strategy_comparison.py::test_strategy_comparison_summary_contains_required_metrics -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/pipeline/strategy_comparison.py` contains both exported functions.
    - Summary output includes all metric names exactly: parse_success_rate, normalized_label_coverage, rerun_consistency, avg_confidence.
    - Default summary path points to `.planning/phases/04-quality-signals-and-evaluation/04-AB-RESULTS.md`.
  </acceptance_criteria>
  <done>QLT-02 comparison engine produces deterministic evidence summary output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add deterministic tests for prompt strategy comparison</name>
  <files>tests/test_prompt_strategy_comparison.py</files>
  <read_first>src/pipeline/misconception_prompt.py, src/pipeline/strategy_comparison.py, tests/test_resilient_misconception_extraction.py, .planning/phases/04-quality-signals-and-evaluation/04-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Prompt builders stay strict JSON and differ between strategies.
    - Test 2: Strategy comparison summary includes required metrics and deterministic repeated results.
  </behavior>
  <action>Create `tests/test_prompt_strategy_comparison.py` with test names exactly `test_prompt_strategy_builders_are_distinct_and_json_strict`, `test_strategy_comparison_summary_contains_required_metrics`, and `test_strategy_comparison_is_deterministic_across_runs`. Use deterministic fixtures and mocked analyzer responses to avoid external API dependence.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_prompt_strategy_comparison.py::test_prompt_strategy_builders_are_distinct_and_json_strict tests/test_prompt_strategy_comparison.py::test_strategy_comparison_summary_contains_required_metrics tests/test_prompt_strategy_comparison.py::test_strategy_comparison_is_deterministic_across_runs -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_prompt_strategy_comparison.py` contains all three required test names.
    - Tests verify required metrics and deterministic repeated output behavior.
    - Targeted command exits with code 0.
  </acceptance_criteria>
  <done>QLT-02 is protected by deterministic strategy-comparison tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prompt strategy config -> comparison metrics | Strategy differences can bias quality evaluation if not normalized |
| comparison output -> planning decisions | Summary artifact influences future prompt choices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T | src/pipeline/strategy_comparison.py | mitigate | Use deterministic ordering and fixed metric definitions to prevent metric tampering ambiguity |
| T-04-04 | R | src/pipeline/strategy_comparison.py | mitigate | Persist required metric names and deterministic summary output for reproducible audit trails |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_prompt_strategy_comparison.py::test_prompt_strategy_builders_are_distinct_and_json_strict tests/test_prompt_strategy_comparison.py::test_strategy_comparison_summary_contains_required_metrics tests/test_prompt_strategy_comparison.py::test_strategy_comparison_is_deterministic_across_runs -q
- [ ] venv\Scripts\python -m pytest tests/test_prompt_strategy_comparison.py -q
</verification>

<success_criteria>
- QLT-02 strategy comparison is implemented with deterministic, auditable summary output
- Strategy metrics are reproducible and usable for future prompt decisions
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-signals-and-evaluation/04-02-SUMMARY.md`
</output>
