---
phase: 03-teacher-report-and-aggregation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/pipeline/report_writer.py
  - src/pipeline/report_pipeline.py
  - tests/test_teacher_report_aggregation.py
autonomous: true
requirements:
  - RPT-02
  - RPT-04
must_haves:
  truths:
    - "User can generate deterministic teacher_report.json output on repeated runs"
    - "User can inspect concise evidence snippets with question_text and student_answer"
  artifacts:
    - path: "src/pipeline/report_writer.py"
      provides: "Deterministic report serialization and file output"
    - path: "src/pipeline/report_pipeline.py"
      provides: "Aggregation-to-report orchestration with metadata counters"
    - path: "tests/test_teacher_report_aggregation.py"
      provides: "Determinism and evidence-snippet verification tests"
  key_links:
    - from: "src/pipeline/report_pipeline.py"
      to: "src/pipeline/report_aggregation.py"
      via: "Consumes phase-01 aggregation functions and produces final report structure"
    - from: "src/pipeline/report_writer.py"
      to: "tests/test_teacher_report_aggregation.py"
      via: "Deterministic JSON output assertions"
---

<objective>
Complete Phase 3 with deterministic report generation and concise evidence snippet output.

Purpose: Produce teacher_report.json that is stable, auditable, and directly useful for teacher review.
Output: Report writer/pipeline modules and tests for RPT-02 and RPT-04.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md
@.planning/phases/03-teacher-report-and-aggregation/03-RESEARCH.md
@.planning/phases/03-teacher-report-and-aggregation/03-VALIDATION.md
@.planning/phases/03-teacher-report-and-aggregation/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic report assembly and writer</name>
  <files>src/pipeline/report_writer.py, src/pipeline/report_pipeline.py</files>
  <read_first>src/pipeline/report_aggregation.py, .planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <behavior>
    - Test 1: Generated report schema is stable across repeated runs with same input.
    - Test 2: JSON serialization ordering and numeric formatting are deterministic.
  </behavior>
  <action>Create src/pipeline/report_pipeline.py with build_teacher_report(rows: list[dict]) -> dict combining student/concept aggregation, cohort summary, and metadata counters (including rows_excluded_retry_exhausted). Create src/pipeline/report_writer.py with write_teacher_report(report: dict, output_path: str = "teacher_report.json") -> None using json.dumps(..., sort_keys=True, ensure_ascii=True, indent=2). Ensure stable list ordering before serialization for students, concepts, misconceptions, and cohort items.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_report_json_is_deterministic -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/report_writer.py contains write_teacher_report and default output path teacher_report.json.
    - src/pipeline/report_pipeline.py contains build_teacher_report with metadata counter rows_excluded_retry_exhausted.
    - deterministic JSON settings include sort_keys=True and ensure_ascii=True.
  </acceptance_criteria>
  <done>RPT-02 deterministic report generation path is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement concise evidence snippet projection</name>
  <files>src/pipeline/report_pipeline.py</files>
  <read_first>src/pipeline/report_pipeline.py, src/pipeline/report_aggregation.py, .planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Evidence snippets include only question_text and student_answer.
    - Test 2: At most 3 snippets are retained per misconception aggregate.
  </behavior>
  <action>Add evidence projection helper in src/pipeline/report_pipeline.py that extracts `question_text` and `student_answer` from included statuses (ok/json_repaired/fallback_used), deduplicates deterministic duplicates, and caps list length to 3 per misconception in both student and cohort sections. Exclude retry_exhausted rows from evidence arrays while preserving metadata counters.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_evidence_snippets_are_concise_and_scoped -q</automated>
  </verify>
  <acceptance_criteria>
    - evidence snippet objects contain only keys question_text and student_answer.
    - evidence snippet arrays are capped at 3 entries per misconception aggregate.
    - retry_exhausted rows do not appear in evidence snippets.
  </acceptance_criteria>
  <done>RPT-04 concise and scoped evidence behavior is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend deterministic report tests for RPT-02 and RPT-04</name>
  <files>tests/test_teacher_report_aggregation.py</files>
  <read_first>src/pipeline/report_pipeline.py, src/pipeline/report_writer.py, tests/test_teacher_report_aggregation.py, .planning/phases/03-teacher-report-and-aggregation/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Repeated report generation for identical input produces byte-identical JSON output.
    - Test 2: Evidence snippets are concise, scoped, and capped.
  </behavior>
  <action>Extend tests/test_teacher_report_aggregation.py with test names exactly: test_report_json_is_deterministic and test_evidence_snippets_are_concise_and_scoped. For determinism, generate report twice from same fixture rows and assert dict equality plus file-content equality from write_teacher_report output. For snippets, assert each snippet has only question_text/student_answer and max length <= 3.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_report_json_is_deterministic tests/test_teacher_report_aggregation.py::test_evidence_snippets_are_concise_and_scoped -q</automated>
  </verify>
  <acceptance_criteria>
    - tests file contains both required test names.
    - determinism test checks repeated generation equality.
    - evidence snippet test enforces allowed keys and max-length cap.
  </acceptance_criteria>
  <done>RPT-02 and RPT-04 are covered with deterministic automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregation outputs -> serialized report | Derived metrics and evidence cross into persisted teacher-facing artifact |
| report artifact -> downstream review workflows | JSON report consumed by later quality and review phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | R | src/pipeline/report_writer.py | mitigate | Deterministic serialization and stable ordering enable reproducible report auditing |
| T-03-04 | I | src/pipeline/report_pipeline.py | mitigate | Restrict evidence snippets to question_text/student_answer and cap length to avoid leaking unrelated fields |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_report_json_is_deterministic tests/test_teacher_report_aggregation.py::test_evidence_snippets_are_concise_and_scoped -q
- [ ] venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py -q
</verification>

<success_criteria>
- RPT-02 and RPT-04 are fully covered by report assembly/writer implementation and tests
- teacher_report.json output is deterministic for identical inputs
- evidence snippets are concise, scoped, and teacher-readable
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-report-and-aggregation/03-02-SUMMARY.md`
</output>
