---
phase: 03-teacher-report-and-aggregation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/report_aggregation.py
  - tests/test_teacher_report_aggregation.py
autonomous: true
requirements:
  - RPT-01
  - RPT-03
must_haves:
  truths:
    - "User can view deterministic mastery scores from 0-100 per student and concept"
    - "User can review deterministic cohort-level top misconceptions by concept"
  artifacts:
    - path: "src/pipeline/report_aggregation.py"
      provides: "Mastery scoring and cohort summary aggregation logic"
    - path: "tests/test_teacher_report_aggregation.py"
      provides: "Automated checks for scoring and ranking behavior"
  key_links:
    - from: "src/pipeline/report_aggregation.py"
      to: "src/pipeline/misconception_extractor.py"
      via: "Consumes analyzed row statuses and misconception arrays"
    - from: "src/pipeline/report_aggregation.py"
      to: "tests/test_teacher_report_aggregation.py"
      via: "Deterministic scoring and ranking assertions"
---

<objective>
Build the aggregation core for Phase 3: deterministic mastery scoring and cohort misconception ranking.

Purpose: Establish reliable teacher-facing insight metrics before report serialization.
Output: Aggregation module and baseline tests for RPT-01 and RPT-03.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md
@.planning/phases/03-teacher-report-and-aggregation/03-RESEARCH.md
@.planning/phases/03-teacher-report-and-aggregation/03-VALIDATION.md
@.planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic mastery scoring by student and concept</name>
  <files>src/pipeline/report_aggregation.py</files>
  <read_first>.planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md, .planning/REQUIREMENTS.md, src/pipeline/misconception_extractor.py, src/pipeline/json_resilience.py</read_first>
  <behavior>
    - Test 1: Mastery score is always an integer in [0, 100].
    - Test 2: Identical inputs produce identical mastery scores.
  </behavior>
  <action>Create src/pipeline/report_aggregation.py with compute_mastery_score(records: list[dict]) -> int using confidence-weighted misconception penalties and deterministic clamp/round behavior. Include aggregate_by_student_concept(rows: list[dict]) -> dict that excludes rows where status == "retry_exhausted" from scoring inputs and tracks excluded row counts. Use explicit status allow-list for scoring: ok, json_repaired, fallback_used.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_mastery_score_is_bounded_and_deterministic -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/report_aggregation.py contains compute_mastery_score and aggregate_by_student_concept functions.
    - status strings "ok", "json_repaired", "fallback_used", and "retry_exhausted" appear in aggregation branch logic.
    - mastery score output is int and clamped to range 0..100.
  </acceptance_criteria>
  <done>RPT-01 aggregation scoring behavior is deterministic and bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic cohort misconception summary ranking</name>
  <files>src/pipeline/report_aggregation.py</files>
  <read_first>src/pipeline/report_aggregation.py, .planning/phases/03-teacher-report-and-aggregation/03-CONTEXT.md, .planning/phases/02-resilient-misconception-extraction/02-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: Top misconceptions are sorted by occurrences desc, avg_confidence desc, label asc.
    - Test 2: Cohort summary includes occurrences, affected_students, and avg_confidence.
  </behavior>
  <action>Add build_cohort_summary(rows: list[dict]) -> dict to src/pipeline/report_aggregation.py. Group by concept and misconception label; compute occurrences, unique affected student count, and average confidence. Sort with explicit tuple key: (-occurrences, -avg_confidence, label). Return deterministic arrays with stable ordering and explicit numeric rounding for avg_confidence.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_cohort_summary_ranking_is_deterministic -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/report_aggregation.py contains build_cohort_summary.
    - cohort summary entries include keys occurrences, affected_students, avg_confidence.
    - sort key behavior matches count/confidence/label ordering.
  </acceptance_criteria>
  <done>RPT-03 cohort summary outputs are deterministic and teacher-actionable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add baseline aggregation tests for RPT-01 and RPT-03</name>
  <files>tests/test_teacher_report_aggregation.py</files>
  <read_first>src/pipeline/report_aggregation.py, .planning/phases/03-teacher-report-and-aggregation/03-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Mastery scoring is deterministic and bounded.
    - Test 2: Cohort ranking order is deterministic with tie-breaks.
  </behavior>
  <action>Create tests/test_teacher_report_aggregation.py with test names exactly: test_mastery_score_is_bounded_and_deterministic and test_cohort_summary_ranking_is_deterministic. Include fixtures mixing statuses ok/json_repaired/fallback_used/retry_exhausted to validate inclusion and exclusion rules. Assert exact rank ordering for at least one tie-break case.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_mastery_score_is_bounded_and_deterministic tests/test_teacher_report_aggregation.py::test_cohort_summary_ranking_is_deterministic -q</automated>
  </verify>
  <acceptance_criteria>
    - tests file contains both required test function names.
    - tests assert deterministic ordering and bounded score behavior.
    - quick test command exits 0.
  </acceptance_criteria>
  <done>RPT-01 and RPT-03 are protected by deterministic automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzed-rows -> aggregation | Untrusted model-derived content crosses into report scoring logic |
| aggregation -> teacher report | Derived metrics are consumed by teacher-facing outputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | src/pipeline/report_aggregation.py | mitigate | Enforce explicit status gating and deterministic score formula to prevent malformed status tampering from polluting metrics |
| T-03-02 | I | src/pipeline/report_aggregation.py | mitigate | Deterministic sort and explicit metric fields prevent inconsistent cohort summaries across reruns |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py::test_mastery_score_is_bounded_and_deterministic tests/test_teacher_report_aggregation.py::test_cohort_summary_ranking_is_deterministic -q
- [ ] venv\Scripts\python -m pytest tests/test_teacher_report_aggregation.py -q
</verification>

<success_criteria>
- RPT-01 and RPT-03 are covered by deterministic aggregation logic and tests
- Mastery scores and cohort rankings are stable across repeated runs
</success_criteria>

<output>
After completion, create `.planning/phases/03-teacher-report-and-aggregation/03-01-SUMMARY.md`
</output>
