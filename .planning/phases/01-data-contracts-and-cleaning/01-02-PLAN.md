---
phase: 01-data-contracts-and-cleaning
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pipeline/filtering.py
  - src/pipeline/drop_log.py
  - src/pipeline/ingest.py
  - tests/test_data_contracts_and_cleaning.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Malformed rows are logged with machine-readable reason metadata"
    - "Only strict incorrect submissions are forwarded for next phase"
  artifacts:
    - path: "src/pipeline/filtering.py"
      provides: "Strict incorrect-attempt filter"
    - path: "src/pipeline/drop_log.py"
      provides: "Structured drop-log writer and summary metrics"
    - path: "src/pipeline/ingest.py"
      provides: "End-to-end ingestion orchestration output"
  key_links:
    - from: "src/pipeline/ingest.py"
      to: "src/pipeline/filtering.py"
      via: "Forward only rows where is_correct == False"
    - from: "src/pipeline/ingest.py"
      to: "src/pipeline/drop_log.py"
      via: "Persist dropped-row reason metadata"
---

<objective>
Complete Phase 1 orchestration by adding strict incorrect filtering and structured malformed-row logging.

Purpose: Ensure downstream misconception analysis receives only valid incorrect attempts with full drop diagnostics.
Output: Filtering module, drop-log module, ingestion orchestrator, and tests for DATA-03 and DATA-04.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md
@.planning/phases/01-data-contracts-and-cleaning/01-RESEARCH.md
@.planning/phases/01-data-contracts-and-cleaning/01-VALIDATION.md
@.planning/phases/01-data-contracts-and-cleaning/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict incorrect-attempt filtering</name>
  <files>src/pipeline/filtering.py</files>
  <read_first>src/pipeline/contracts.py, src/pipeline/cleaning.py, .planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Rows with is_correct=False are forwarded.
    - Test 2: Rows with is_correct=True are excluded from downstream payload.
    - Test 3: Rows with non-boolean/missing is_correct are malformed with reason_code=invalid_is_correct.
  </behavior>
  <action>Create src/pipeline/filtering.py with filter_incorrect_rows(rows: list[dict]) -> list[dict] and validate_is_correct(value) -> tuple[bool, str | None]. Accept only strict bool values for is_correct. Return reason code "invalid_is_correct" for missing/non-boolean values. Forward only rows with is_correct equal to False; never coerce strings like "false".</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py::test_only_incorrect_rows_forwarded tests/test_data_contracts_and_cleaning.py::test_invalid_is_correct_rows_are_dropped -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/filtering.py contains reason code "invalid_is_correct".
    - src/pipeline/filtering.py forwards only rows where is_correct is strict False.
    - src/pipeline/filtering.py does not coerce non-boolean string flags.
  </acceptance_criteria>
  <done>Filtering logic enforces DATA-04 rules and prevents invalid correctness flags from leaking downstream.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured drop logging and ingest orchestration</name>
  <files>src/pipeline/drop_log.py, src/pipeline/ingest.py</files>
  <read_first>src/pipeline/contracts.py, src/pipeline/cleaning.py, src/pipeline/filtering.py, .planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Drop log file writes one JSON/JSONL record per dropped row with reason metadata.
    - Test 2: Console summary reports total, valid, dropped, and per-reason counts.
  </behavior>
  <action>Create src/pipeline/drop_log.py with write_drop_log(records, output_path="artifacts/drop_log.jsonl") and summarize_drops(records). Each log record must include row_index, reason_code, and field (when available). Create src/pipeline/ingest.py with run_ingestion_pipeline(input_path) that validates, cleans, filters incorrect rows, writes drop log, prints summary, and returns cleaned_incorrect_rows plus summary dict.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py::test_drop_log_written_with_reason_codes tests/test_data_contracts_and_cleaning.py::test_console_summary_has_reason_counts -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/drop_log.py writes structured log records with keys row_index and reason_code.
    - src/pipeline/ingest.py prints summary containing total_rows, valid_rows, dropped_rows, reason_counts.
    - Default log output path contains "artifacts/drop_log.jsonl".
  </acceptance_criteria>
  <done>Malformed-drop diagnostics are available in both human-readable summary and machine-readable artifact for DATA-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend tests for DATA-03 and DATA-04 end-to-end behavior</name>
  <files>tests/test_data_contracts_and_cleaning.py</files>
  <read_first>src/pipeline/filtering.py, src/pipeline/drop_log.py, src/pipeline/ingest.py, .planning/phases/01-data-contracts-and-cleaning/01-VALIDATION.md</read_first>
  <behavior>
    - Test 1: invalid is_correct rows are dropped and logged.
    - Test 2: drop-log artifact contains expected reason codes and metadata.
    - Test 3: pipeline output includes only normalized incorrect rows.
  </behavior>
  <action>Update tests/test_data_contracts_and_cleaning.py with test_invalid_is_correct_rows_are_dropped, test_only_incorrect_rows_forwarded, test_drop_log_written_with_reason_codes, and test_console_summary_has_reason_counts. Add an end-to-end test invoking run_ingestion_pipeline on fixture rows and asserting returned rows are all is_correct=False with normalized timestamps.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests file contains all four required DATA-03/04 test names.
    - End-to-end test asserts returned pipeline rows are strict incorrect submissions only.
    - Phase test suite exits 0 from venv pytest command.
  </acceptance_criteria>
  <done>DATA-03 and DATA-04 are proven through deterministic automated tests and integrated pipeline behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleaned-rows -> filter | Validated rows cross into correctness-based routing |
| drop-events -> log-artifact | Error metadata becomes persistent audit artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | T | src/pipeline/filtering.py | mitigate | Enforce strict boolean semantics for is_correct; drop and log invalid values using invalid_is_correct |
| T-01-04 | R | src/pipeline/drop_log.py | mitigate | Persist row_index + reason_code records and summary counters to provide non-repudiable malformed-row audit trail |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py -q
- [ ] Pipeline run emits artifacts/drop_log.jsonl with structured reason metadata
</verification>

<success_criteria>
- DATA-03 and DATA-04 requirements are fully covered
- Structured drop logging and strict incorrect filtering are integrated in ingestion flow
- All listed automated checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-contracts-and-cleaning/01-02-SUMMARY.md`
</output>
