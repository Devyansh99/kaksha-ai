---
phase: 01-data-contracts-and-cleaning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/contracts.py
  - src/pipeline/cleaning.py
  - tests/test_data_contracts_and_cleaning.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Ingestion rejects rows that violate required schema contract"
    - "Accepted timestamps are normalized to UTC ISO-8601"
  artifacts:
    - path: "src/pipeline/contracts.py"
      provides: "Required field contract and row validation"
    - path: "src/pipeline/cleaning.py"
      provides: "Timestamp parsing and normalization pipeline"
    - path: "tests/test_data_contracts_and_cleaning.py"
      provides: "Automated checks for DATA-01 and DATA-02"
  key_links:
    - from: "src/pipeline/contracts.py"
      to: "src/pipeline/cleaning.py"
      via: "Validated row object passed into cleaning transform"
    - from: "src/pipeline/cleaning.py"
      to: "tests/test_data_contracts_and_cleaning.py"
      via: "Deterministic UTC timestamp assertions"
---

<objective>
Create the Phase 1 contract and cleaning foundation that enforces strict required fields and deterministic timestamp normalization.

Purpose: Establish fail-closed data quality guarantees before any misconception analysis.
Output: Reusable contract validators, timestamp normalization logic, and tests for DATA-01 and DATA-02.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md
@.planning/phases/01-data-contracts-and-cleaning/01-RESEARCH.md
@.planning/phases/01-data-contracts-and-cleaning/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict schema contract validator</name>
  <files>src/pipeline/contracts.py</files>
  <read_first>.planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md, .planning/REQUIREMENTS.md, prd.md</read_first>
  <behavior>
    - Test 1: Missing any required field yields malformed result with reason_code=missing_required_field.
    - Test 2: Non-string values for textual fields yield malformed result with reason_code=invalid_field_type.
  </behavior>
  <action>Create src/pipeline/contracts.py with REQUIRED_FIELDS list exactly: student_id, subject, concept, question_text, correct_answer, student_answer, is_correct, timestamp. Add validate_row_contract(row: dict) -> tuple[bool, str | None] that returns (False, reason_code) for missing fields, null-like values, and invalid base types; return (True, None) only when row passes contract checks. Use explicit reason codes: missing_required_field, invalid_field_type.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py::test_missing_required_fields_are_dropped tests/test_data_contracts_and_cleaning.py::test_invalid_field_types_are_dropped -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/contracts.py contains REQUIRED_FIELDS with all 8 required column names.
    - src/pipeline/contracts.py contains function signature validate_row_contract(row: dict).
    - src/pipeline/contracts.py contains reason codes "missing_required_field" and "invalid_field_type".
  </acceptance_criteria>
  <done>Contract validator rejects malformed rows with deterministic reason codes and passes valid rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement timestamp normalization to UTC ISO-8601</name>
  <files>src/pipeline/cleaning.py</files>
  <read_first>src/pipeline/contracts.py, .planning/phases/01-data-contracts-and-cleaning/01-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Parseable timestamps normalize to UTC ISO-8601 with trailing Z.
    - Test 2: Unparseable timestamps are rejected with reason_code=invalid_timestamp.
  </behavior>
  <action>Create src/pipeline/cleaning.py with normalize_timestamp(raw_timestamp: str) -> tuple[str | None, str | None]. Support common ISO/date-like inputs, convert to timezone-aware UTC, and output ISO-8601 string (YYYY-MM-DDTHH:MM:SSZ). Return (None, "invalid_timestamp") when parsing fails. Add clean_row(row: dict) that applies contract validation + timestamp normalization and returns normalized row or drop reason.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py::test_timestamp_normalization_to_utc_iso tests/test_data_contracts_and_cleaning.py::test_invalid_timestamp_is_dropped -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/cleaning.py contains normalize_timestamp(raw_timestamp: str).
    - src/pipeline/cleaning.py contains reason code "invalid_timestamp".
    - src/pipeline/cleaning.py serializes accepted timestamps in UTC ISO-8601 format.
  </acceptance_criteria>
  <done>Cleaning module outputs deterministic UTC timestamps and fail-closed behavior for invalid timestamps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 1 contract and timestamp tests</name>
  <files>tests/test_data_contracts_and_cleaning.py</files>
  <read_first>src/pipeline/contracts.py, src/pipeline/cleaning.py, .planning/phases/01-data-contracts-and-cleaning/01-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Missing required fields and invalid types are dropped with expected reason codes.
    - Test 2: Valid timestamps normalize to UTC ISO-8601; invalid timestamps are dropped.
  </behavior>
  <action>Create tests/test_data_contracts_and_cleaning.py with explicit fixtures and assertions for DATA-01 and DATA-02 behaviors. Add test names exactly: test_missing_required_fields_are_dropped, test_invalid_field_types_are_dropped, test_timestamp_normalization_to_utc_iso, test_invalid_timestamp_is_dropped. Ensure tests assert exact reason-code strings.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_data_contracts_and_cleaning.py contains all 4 required test function names.
    - tests assert exact reason codes and UTC normalization behavior.
    - Local phase test command exits 0.
  </acceptance_criteria>
  <done>Automated contract/timestamp tests prove DATA-01 and DATA-02 behavior end-to-end for plan scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input-file -> validator | Untrusted CSV/JSON row data enters contract checks |
| parser -> normalized-output | Parsed timestamps cross into trusted normalized artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | src/pipeline/contracts.py | mitigate | Reject malformed/typed rows using explicit required-field and reason-code checks before transform |
| T-01-02 | I | src/pipeline/cleaning.py | mitigate | Fail-closed timestamp parsing; only emit UTC ISO-8601 from successfully parsed timezone-aware values |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_data_contracts_and_cleaning.py -q
- [ ] Validation report confirms DATA-01 and DATA-02 test rows pass/fail as expected
</verification>

<success_criteria>
- DATA-01 and DATA-02 requirements are covered by code and tests
- All listed automated checks pass
- Contract and timestamp behavior is deterministic and fail-closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-contracts-and-cleaning/01-01-SUMMARY.md`
</output>
