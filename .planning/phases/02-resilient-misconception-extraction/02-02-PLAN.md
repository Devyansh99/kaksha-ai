---
phase: 02-resilient-misconception-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/pipeline/json_resilience.py
  - src/pipeline/misconception_extractor.py
  - tests/test_resilient_misconception_extraction.py
autonomous: true
requirements:
  - LLM-03
  - LLM-04
must_haves:
  truths:
    - "Timeout and service failures do not crash extraction and are handled by bounded retries"
    - "Malformed model JSON is repaired once, then deterministic fallback is used when needed"
  artifacts:
    - path: "src/pipeline/json_resilience.py"
      provides: "JSON parse/repair and fallback routing helpers"
    - path: "src/pipeline/misconception_extractor.py"
      provides: "Per-row resilient extraction orchestration"
    - path: "tests/test_resilient_misconception_extraction.py"
      provides: "Automated resilience and fallback coverage"
  key_links:
    - from: "src/pipeline/misconception_extractor.py"
      to: "src/pipeline/openrouter_client.py"
      via: "Bounded retry loop around model invocation"
    - from: "src/pipeline/misconception_extractor.py"
      to: "src/pipeline/json_resilience.py"
      via: "Parse/repair/fallback decision path"
---

<objective>
Implement resilient misconception extraction runtime with bounded retry and deterministic malformed-JSON fallback.

Purpose: Guarantee analyzable outputs for incorrect submissions even when model/API responses are unstable.
Output: JSON resilience module, extraction orchestrator, and tests for LLM-03 and LLM-04.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md
@.planning/phases/02-resilient-misconception-extraction/02-RESEARCH.md
@.planning/phases/02-resilient-misconception-extraction/02-VALIDATION.md
@.planning/phases/02-resilient-misconception-extraction/02-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement JSON parse-repair-fallback helpers</name>
  <files>src/pipeline/json_resilience.py</files>
  <read_first>src/pipeline/misconception_prompt.py, .planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md, .planning/phases/02-resilient-misconception-extraction/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Valid JSON payload parses directly into expected dict structure.
    - Test 2: Single malformed JSON variant can be repaired once and parsed.
    - Test 3: Unrepairable payload returns fallback-needed status.
  </behavior>
  <action>Create src/pipeline/json_resilience.py with parse_or_repair_json(raw_text: str) -> tuple[dict | None, str] where status values are exactly: ok, json_repaired, fallback_used. Implement one repair attempt only using concrete transforms: strip markdown code fences, trim trailing commas before closing braces/brackets, and isolate first JSON object span. Add deterministic_keyword_fallback(row: dict) -> dict that emits the same schema shape with source="fallback" and confidence defaults (0.2) based on simple keyword mapping in student_answer.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_malformed_json_repair_then_fallback -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/json_resilience.py contains status literals ok, json_repaired, fallback_used.
    - parse_or_repair_json performs at most one repair pass before fallback status.
    - deterministic_keyword_fallback returns misconceptions array with label, rationale, confidence keys.
  </acceptance_criteria>
  <done>Malformed JSON handling is deterministic and bounded, satisfying the repair/fallback contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resilient per-row extraction orchestrator</name>
  <files>src/pipeline/misconception_extractor.py</files>
  <read_first>src/pipeline/openrouter_client.py, src/pipeline/json_resilience.py, src/pipeline/misconception_prompt.py, src/pipeline/ingest.py</read_first>
  <behavior>
    - Test 1: Timeouts/service failures retry up to configured max_retries, then return structured exhaustion status.
    - Test 2: Successful response path returns source=llm with parsed misconceptions payload.
    - Test 3: JSON failure path calls repair helper and then fallback helper if still invalid.
  </behavior>
  <action>Create src/pipeline/misconception_extractor.py with extract_misconceptions_for_row(row: dict, config: OpenRouterConfig) -> dict and extract_for_incorrect_rows(rows: list[dict], config: OpenRouterConfig) -> list[dict]. Implement deterministic retry loop with concrete behavior: attempt numbers 1..max_retries+1, timeout exception codes mapped to error_code="timeout", HTTP/service failures mapped to error_code="service_error", and stop after max_retries returning status="retry_exhausted" instead of raising. On each successful raw response, route through parse_or_repair_json; if fallback_used, merge deterministic fallback output while preserving student_id/concept/question_text/student_answer.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_retry_timeout_is_bounded_and_non_crashing -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/misconception_extractor.py contains extract_misconceptions_for_row and extract_for_incorrect_rows.
    - Retry loop upper bound is driven by config.max_retries and returns status retry_exhausted on exhaustion.
    - Returned records include source, status, and error_code fields on failure paths.
  </acceptance_criteria>
  <done>Extraction orchestration is resilient, bounded, and non-crashing for LLM-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add resilience and fallback test coverage</name>
  <files>tests/test_resilient_misconception_extraction.py</files>
  <read_first>src/pipeline/json_resilience.py, src/pipeline/misconception_extractor.py, src/pipeline/openrouter_client.py, .planning/phases/02-resilient-misconception-extraction/02-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Timeout path retries bounded count and exits with retry_exhausted status.
    - Test 2: Malformed JSON path attempts repair once then falls back deterministically.
  </behavior>
  <action>Extend tests/test_resilient_misconception_extraction.py with exact test names: test_retry_timeout_is_bounded_and_non_crashing and test_malformed_json_repair_then_fallback. Stub call_openrouter to simulate timeout and malformed JSON scenarios. Assert exact status values retry_exhausted, json_repaired, and fallback_used as applicable, and assert fallback output keeps required schema keys.
</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_retry_timeout_is_bounded_and_non_crashing tests/test_resilient_misconception_extraction.py::test_malformed_json_repair_then_fallback -q</automated>
  </verify>
  <acceptance_criteria>
    - tests file contains both required resilience test names.
    - Tests assert bounded retry count and non-crashing exhaustion behavior.
    - Tests assert deterministic fallback schema parity with normal extraction output.
  </acceptance_criteria>
  <done>LLM-03 and LLM-04 are validated with deterministic automated resilience tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime -> external LLM API | Unreliable remote service boundary can return malformed or adversarial content |
| model output -> parser/fallback | Untrusted text crosses into structured parsing pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | D | src/pipeline/misconception_extractor.py | mitigate | Bounded retries and explicit timeout handling prevent infinite hangs and service-induced denial of progress |
| T-02-04 | T | src/pipeline/json_resilience.py | mitigate | One-pass repair plus deterministic fallback avoids unsafe parser loops and malformed-output crashes |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_retry_timeout_is_bounded_and_non_crashing tests/test_resilient_misconception_extraction.py::test_malformed_json_repair_then_fallback -q
- [ ] venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py -q
</verification>

<success_criteria>
- LLM-03 and LLM-04 requirements are covered by bounded retry and repair/fallback logic
- All resilience tests pass with deterministic status outputs
- Extraction path returns structured non-crashing outputs for both success and failure conditions
</success_criteria>

<output>
After completion, create `.planning/phases/02-resilient-misconception-extraction/02-02-SUMMARY.md`
</output>
