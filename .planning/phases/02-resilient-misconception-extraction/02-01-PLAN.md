---
phase: 02-resilient-misconception-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline/misconception_prompt.py
  - src/pipeline/openrouter_client.py
  - tests/test_resilient_misconception_extraction.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
must_haves:
  truths:
    - "Each incorrect submission can be transformed into a strict JSON-only LLM prompt"
    - "OpenRouter calls are configured through environment variables and centralized in one client module"
  artifacts:
    - path: "src/pipeline/misconception_prompt.py"
      provides: "JSON-only prompt contract and response schema guidance"
    - path: "src/pipeline/openrouter_client.py"
      provides: "OpenRouter request client with env-based config"
    - path: "tests/test_resilient_misconception_extraction.py"
      provides: "Automated checks for prompt contract and env-var client configuration"
  key_links:
    - from: "src/pipeline/misconception_prompt.py"
      to: "src/pipeline/openrouter_client.py"
      via: "Prompt payload content sent as user input to model call"
    - from: "src/pipeline/openrouter_client.py"
      to: "tests/test_resilient_misconception_extraction.py"
      via: "Client config and request-shape assertions"
---

<objective>
Build the phase foundation for strict JSON misconception extraction and centralized OpenRouter configuration.

Purpose: Lock down deterministic input/output contracts before adding retry/repair orchestration.
Output: Prompt contract module, OpenRouter client module, and baseline tests for LLM-01 and LLM-02.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md
@.planning/phases/02-resilient-misconception-extraction/02-RESEARCH.md
@.planning/phases/02-resilient-misconception-extraction/02-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict JSON-only prompt contract</name>
  <files>src/pipeline/misconception_prompt.py</files>
  <read_first>.planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md, .planning/REQUIREMENTS.md, prd.md</read_first>
  <behavior>
    - Test 1: Prompt text explicitly instructs output as JSON object only with no markdown/prose wrapper.
    - Test 2: Prompt contract includes required top-level keys and misconception-item keys.
  </behavior>
  <action>Create src/pipeline/misconception_prompt.py with build_misconception_prompt(row: dict) -> str and REQUIRED_RESPONSE_KEYS exactly: student_id, concept, question_text, student_answer, misconceptions. Define REQUIRED_MISCONCEPTION_KEYS exactly: label, rationale, confidence. Prompt must include explicit instruction "Return only valid JSON" and include required schema fields in-line as concrete key names. Do not include placeholder references like "see schema elsewhere".</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_prompt_requires_json_only_contract -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/misconception_prompt.py contains REQUIRED_RESPONSE_KEYS with all five key names.
    - src/pipeline/misconception_prompt.py contains REQUIRED_MISCONCEPTION_KEYS with label, rationale, confidence.
    - build_misconception_prompt includes the exact phrase "Return only valid JSON".
  </acceptance_criteria>
  <done>Prompt builder enforces deterministic JSON-only output contract for LLM-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement centralized OpenRouter client with env configuration</name>
  <files>src/pipeline/openrouter_client.py</files>
  <read_first>src/pipeline/misconception_prompt.py, .planning/phases/02-resilient-misconception-extraction/02-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Client reads OPENROUTER_API_KEY and OPENROUTER_MODEL from environment.
    - Test 2: Request payload uses centralized defaults for timeout, temperature, and max_tokens.
  </behavior>
  <action>Create src/pipeline/openrouter_client.py with OpenRouterConfig dataclass fields: api_key, model, timeout_seconds, max_retries, backoff_seconds, temperature, max_tokens. Add load_openrouter_config() reading OPENROUTER_API_KEY and OPENROUTER_MODEL with explicit ValueError if missing key. Add call_openrouter(prompt: str, config: OpenRouterConfig) -> dict using urllib.request with Authorization header format "Bearer {api_key}" and endpoint https://openrouter.ai/api/v1/chat/completions. Keep all HTTP defaults centralized in this file only.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_openrouter_client_uses_env_config -q</automated>
  </verify>
  <acceptance_criteria>
    - src/pipeline/openrouter_client.py contains load_openrouter_config and call_openrouter functions.
    - OPENROUTER_API_KEY and OPENROUTER_MODEL strings appear in config loader.
    - OpenRouter endpoint string "/api/v1/chat/completions" appears once in client module.
  </acceptance_criteria>
  <done>OpenRouter configuration is env-driven and centrally managed for LLM-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add baseline tests for prompt and client contract</name>
  <files>tests/test_resilient_misconception_extraction.py</files>
  <read_first>src/pipeline/misconception_prompt.py, src/pipeline/openrouter_client.py, .planning/phases/02-resilient-misconception-extraction/02-VALIDATION.md</read_first>
  <behavior>
    - Test 1: Prompt includes required key names and JSON-only constraint.
    - Test 2: Config loader and request payload use env-backed values.
  </behavior>
  <action>Create tests/test_resilient_misconception_extraction.py with test names exactly: test_prompt_requires_json_only_contract and test_openrouter_client_uses_env_config. Use monkeypatch to set env vars and assert config fields and request body values include the chosen model and the prompt content. Keep tests network-isolated by stubbing HTTP send path.</action>
  <verify>
    <automated>venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_prompt_requires_json_only_contract tests/test_resilient_misconception_extraction.py::test_openrouter_client_uses_env_config -q</automated>
  </verify>
  <acceptance_criteria>
    - tests file contains both required test function names.
    - Tests assert JSON-only prompt phrase and required response keys.
    - Tests run without external network dependency.
  </acceptance_criteria>
  <done>LLM-01 and LLM-02 behavior is covered by deterministic unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cleaned-incorrect-row -> LLM prompt | Untrusted student answer content crosses into model prompt construction |
| application -> OpenRouter API | Outbound call to third-party service with auth token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | src/pipeline/misconception_prompt.py | mitigate | Enforce strict JSON-only output contract and fixed schema key list to reduce prompt-output tampering and parser ambiguity |
| T-02-02 | I | src/pipeline/openrouter_client.py | mitigate | Keep API key in env vars only and use explicit authorization header; never persist secrets in logs or code |
</threat_model>

<verification>
- [ ] venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py::test_prompt_requires_json_only_contract tests/test_resilient_misconception_extraction.py::test_openrouter_client_uses_env_config -q
- [ ] venv\Scripts\python -m pytest tests/test_resilient_misconception_extraction.py -q
</verification>

<success_criteria>
- LLM-01 and LLM-02 requirements are fully represented in code contracts and tests
- OpenRouter setup is centralized and env-configurable
- Prompt contract is strict JSON-only with explicit required fields
</success_criteria>

<output>
After completion, create `.planning/phases/02-resilient-misconception-extraction/02-01-SUMMARY.md`
</output>
